# Token 消耗详解（bili_agent 自动刷视频）

> 宣传/答疑用文档，解答「刷单个视频」和「一整天刷视频」各消耗多少 token。

---

## 一、核心结论（先说重点）

**刷视频时抓字幕、弹幕、评论都是 B站 API 直接拉取，不消耗 LLM token。**
真正的 token 消耗只在少量 LLM 调用点，且识图走的是智谱 GLM-4V-Flash（免费额度大）。

| 场景 | 单个视频 | 一整天（每天最多30轮） |
| --- | --- | --- |
| 普通刷视频（只看文字） | 0 ~ 400 token | 6k ~ 10k token |
| 刷视频 + 识图看画面 | 3,200 ~ 4,200 token | 40k ~ 110k token |

> 以上为估算值，实际随视频长度、弹幕量、所选模型略有浮动。

---

## 二、单个视频的 token 消耗明细

### 场景 A：普通刷视频（只看文字，不识别画面）

| 环节 | 是否耗 LLM token | 说明 |
| --- | --- | --- |
| 抓取字幕/弹幕/评论 | 0 | B站 API 直接拉取，不走 LLM |
| 视频评分 _score_video | 0 | 本地规则打分，非 LLM |
| 评论互动（可选） | 约 350 | 1 次 LLM：输入 ~300 + 输出 ~50 |

**合计：0 ~ 400 token / 视频**

### 场景 B：刷视频 + 识图看画面（判定为「喜欢」才触发）

| 环节 | 是否耗 LLM token | 说明 |
| --- | --- | --- |
| 上面文字部分 | ~400 | 同上 |
| LLM 判断喜不喜欢 | ~300 | 输入标题/简介/评分 + 输出 1 词 |
| LLM 挑识图时间点 | ~500 | 输入弹幕上下文 + 输出时间点 |
| 智谱 GLM-4V 识图 | 2,000~3,000 | 图片 token + 文字输出 |

**合计：约 3,200 ~ 4,200 token / 视频**

---

## 三、一整天刷视频的 token 消耗

**默认配置：** 每 30 分钟刷一轮，每天最多 30 轮（max_daily_browse，可调）。

### 纯文字模式（全天）
- 30 轮 × 平均 ~200 token = **约 6,000 token（6k）**
- 若每轮都评论互动 = 30 × 350 = **约 10,500 token（10k）**

### 全识图模式（全天，假设每轮都识图）
- 30 轮 × ~3,700 token = **约 111,000 token（110k）**
- **实际不会每轮都识图**：LLM 判断后不喜欢的视频会直接跳过
- 若只有 1/3 触发识图 = **约 40k token**

---

## 四、省 token 的关键设计

1. **识图走智谱 GLM-4V-Flash** —— 免费额度大，不占用主模型（deepseek/glm）额度
2. **每日有上限**（默认 30 轮）—— 不会无限刷，token 有明确上限
3. **LLM 判断制** —— 先判断视频值不值得看，不喜欢的直接跳过，省 token 又聪明
4. **文字部分消耗极小** —— 抓取走 API，评分走规则

---

## 五、一句话宣传语

> 「普通刷一整天约 10k token，开识图约 40~110k token，而且识图用的是免费的智谱模型。」

---

## 相关配置项

| 配置 | 默认值 | 说明 |
| --- | --- | --- |
| browse.auto_browse_interval_minutes | 30 | 刷视频间隔（分钟） |
| browse.max_daily_browse | 30 | 每日浏览上限 |
| browse.auto_vision_enabled | true | 是否启用自动识图 |
| browse.auto_vision_interval | 1 | 识图触发（LLM判断制下基本每次判断） |

---

*生成：洛天依 · 2026-08-26*
